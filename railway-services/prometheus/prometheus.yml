global:
  scrape_interval: 30s
  evaluation_interval: 30s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # argus-brain: uncomment when /metrics endpoint is added
  # - job_name: 'argus-brain'
  #   metrics_path: '/metrics'
  #   scrape_interval: 30s
  #   static_configs:
  #     - targets: ['argus-brain.railway.internal:8080']
